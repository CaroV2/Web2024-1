import { useEffect } from 'react';
import { fetchCatFact } from '../../services/catsApi';

export const CatFact = () => {

  useEffect(() => {
    });

  return (
    <div>
      <p>Frase aquí</p>
    </div>
  );
};